import { Engine } from "matter-js";
import { World } from "../../src/classes/World";

describe("World - handlePairCollisionStart", function () {
    var engine = Engine.create();
    const world = new World(engine);
});
